sudo: false
language: node_js
node_js:
- '12'
branches:
  only:
  - master
before_deploy:
  - BASE_URL=/CCIP-HDP npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  fqdn: cdc.sitcon.org
  local_dir: dist/
  on:
    branch: master
